#include <stdio.h>

int main() {
   int a,b,c;
   printf("Enter the three angles of a Triangle");
   scanf("%d %d %d",&a,&b,&c);
   if(a+b+c==180)
   {
       printf("It is a valid Triangle");
   }
   else
   {
       printf("Not a Valid Triangle");
   }
}
