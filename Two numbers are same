#include <stdio.h>

void main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if(a==b)
    {
        printf("The Given two numbers are same");
    }
    else
    {
        printf("The Given two numbers are different");
    }
   
}
